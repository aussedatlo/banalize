{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "@banalize/core#build": {
      "dependsOn": ["^build"],
      "outputs": ["../../target/release/banalize-core"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "inputs": [
        "src/**/*.ts",
        "src/**/*.tsx",
        "src/**/*.rs",
        "*.toml",
        "*.json",
        "*.config.*"
      ]
    },
    "clean": {
      "cache": false
    }
  }
}

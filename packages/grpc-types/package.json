{
  "name": "@banalize/grpc-types",
  "version": "1.0.0",
  "description": "gRPC type definitions for Banalize",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsx scripts/generate-types.ts && tsc",
    "clean": "rm -rf dist"
  },
  "files": [
    "dist",
    "proto"
  ],
  "devDependencies": {
    "tsx": "^4.7.0",
    "typescript": "catalog:"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.10.0"
  }
}

